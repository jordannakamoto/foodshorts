module.exports = {
    darkMode: 'class', // or 'media', doesn't matter — we're overriding it
    content: [
      './app/**/*.{js,ts,jsx,tsx}',
      './components/**/*.{js,ts,jsx,tsx}',
    ],
    theme: {
      extend: {},
    },
    plugins: [],
  }